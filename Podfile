# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'GithubClient-native-apps-IOS' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for GithubClient-native-apps-IOS
  pod 'RealmSwift'
  pod 'GithubAPI'
  pod 'SwiftKeychainWrapper', '~>3.0.1'
  pod 'Siesta/UI', '~>1.0'
  pod 'AeroGearOAuth2'
  pod 'OAuthSwift', '~> 1.2.0'

  target 'GithubClient-native-apps-IOSTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'GithubClient-native-apps-IOSUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
